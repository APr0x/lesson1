def get_multiplied_digits(number):
    str_number = (str(number))
    first = int(str_number[0])
    result = first * get_multiplied_digits(int(str_number[1:]))
    if first < 1:
        return result
    else:
        return first


get_multiplied_digits(8096709)
